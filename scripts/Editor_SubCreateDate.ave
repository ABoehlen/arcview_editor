' Subroutine creates a string from the current date/time
' a shift which will be subtracted can by given as argument
' 10.01.2026, A. Boehlen
'###############################

i =  Self.Get(0)

timestamp = Date.Now - i.AsSeconds
timestamp.SetFormat("yyyyMMddhhhhms")
timestampStr = timestamp.asString
return timestampStr

